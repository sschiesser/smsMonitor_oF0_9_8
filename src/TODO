Resolve screen resolution issue
In BluetoothHelper::loadDevices check if devices with identifier == null are found. (Do these devices have to be added to the ui list as well?
Decouple objective C code from ofApp and make ofApp a C++ class again
make UI beautiful
Device huge methods in ofApp.cpp up into multiple smaller ones
Systemtime

************** from Sébastien :-) ******************
(-buttons)
- OSC settings (same by default but changeable)
(- no need for packet reception statistics)



Done:
- link quality
- battery level
Separate sensors & remote windows to display independant values:

